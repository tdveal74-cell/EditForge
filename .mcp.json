{
  "mcpServers": {
    "editforge": {
      "type": "http",
      "url": "https://editforge.online/api/mcp",
      "headers": {
        "Authorization": "Bearer ${EDITFORGE_MCP_TOKEN:-}"
      }
    },
    "hostinger": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "hostinger-api-mcp"
      ],
      "env": {
        "HOSTINGER_API_TOKEN": "${HOSTINGER_API_TOKEN:-}"
      }
    }
  }
}
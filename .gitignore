node_modules
.next
out
dist
.env
.env.local
.data
.data-test-*
*.log
.DS_Store
*.tsbuildinfo
__pycache__/

# Renders. scripts/render-node01.mjs rebuilds them from the assembly.
/out/

# Private source media mounted into the local stack (compose.local.yaml).
/media/

# Local no-Docker run state (scripts/devon-local-nodocker.sh).
/.local-run/
